# Vertex SuperBot configuration
# Vertex AI relies on Application Default Credentials (ADC).
# Run `gcloud auth application-default login` locally if ADC is not yet configured.

GCP_PROJECT_ID="your-gcp-project-id"
GCP_LOCATION="us-central1"
VERTEX_MODEL="gemini-1.5-flash-001"
VERTEX_TEMPERATURE="0.2"
VERTEX_MAX_OUTPUT_TOKENS="1024"

# Optional logging overrides for CLI/notebook usage
SUPERBOT_LOG_LEVEL="INFO"

